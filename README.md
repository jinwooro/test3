# For testing purpose

The repository contains all the files needed to build a container image on the local machine.
This repo serves as a minimal example of pulling a GitHub repo and building a container image.
